# サイト基本設計書 再帰レビュー

| 項目 | 内容 |
|---|---|
| 対象 | `docs/サイト設計/サイト基本設計書.md` |
| レビュー日時 | 2026-04-26 01:05:19 |
| 参照要件 | `docs/要件定義/サイト要件定義書.md` v1.5.5 |
| 参照ロードマップ | `docs/archive/TODO_開発ロードマップ.md` v1.3.6（archive済み）、`docs/ロードマップ/web_mock実装ロードマップ.md` v1.0.2、`docs/TODO_先行確認ロードマップ.md` v1.0.0（削除済み）|
| 結果 | 指摘事項なし |

---

## 1. 反映済み指摘

| ラウンド | 指摘 | 反映内容 |
|---|---|---|
| 1 | 参照要件が v1.5.4 のまま | v1.5.5 へ更新し、参照ロードマップ行を追加 |
| 1 | 開発ロードマップの現在フェーズと先行確認方針が不足 | フェーズ1確認待ち、先行確認範囲、対象外作業を追記 |
| 1 | フェーズ4とフェーズ5の検証境界が曖昧 | フェーズ4はローカル/静的成果物、フェーズ5は `ConoHa WING Staging` と明記 |
| 1 | `BASE` 外部リンクの最小確認条件が不足 | `baseProductUrl`、未設定時フォールバック、`lastVerifiedAt` を追記 |
| 1 | `DecapCMS` の管理範囲が不足 | SEO補助情報専用であり、本文・商品・通知先・法務本文の正本にしないことを追記 |
| 2 | 商品一覧の絞り込み・並び順の具体方針が不足 | カテゴリ、価格帯、用途タグ、並び順、在庫絞り込み対象外を追記 |
| 2 | 問い合わせフォーム検証とログ保存方針が不足 | 入力項目、必須条件、本文1,000文字、ログ保存範囲、保存期間目安を追記 |
| 2 | GA4イベントと画像SEOの具体方針が不足 | 主要イベント、画像alt、OGP画像、Static Export前提の画像最適化方針を追記 |
| 3 | `DecapCMS` Git運用、静的データ配置、セキュリティヘッダーが不足 | Git backend、認証、更新ブランチ、静的データ配置、手動同期、ヘッダー方針を追記 |

---

## 2. 最終レビュー

- 要件定義書 v1.5.5 の現行方針（`DecapCMS` SEO専用、`BASE` 外部EC、Static Export、`ConoHa WING + PHP` 問い合わせ）と矛盾する記述は検出なし
- 開発ロードマップの現行状態（フェーズ1確認待ち、先行確認作業）と矛盾する記述は検出なし
- 初期リリース方針に反する旧 `microCMS` 初期利用、旧 `Shopify` 初期利用、サイト内カート前提、`preview/` 別アプリ作成前提は検出なし
- `git diff --check` で空白エラーは検出なし

---

*以上*
