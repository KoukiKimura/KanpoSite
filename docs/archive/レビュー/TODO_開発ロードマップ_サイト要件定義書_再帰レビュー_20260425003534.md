# TODO_開発ロードマップ・サイト要件定義書 再帰レビュー結果

| 項目 | 内容 |
|---|---|
| レビュー日時 | 2026-04-25 00:35:34 |
| 主対象 | `docs/TODO_開発ロードマップ.md`, `docs/要件定義/サイト要件定義書.md` |
| 追跡対象 | `README.md`, `Agent.md`, `docs/README.md`, `docs/TODO_web_mock実装ロードマップ.md`, `docs/サイト設計/サイト基本設計書.md`, `docs/サイト設計/サイト詳細設計書.md`, `web_mock/README.md` |
| レビュー種別 | 指摘修正後の再帰レビュー |
| 結果 | 残指摘なし |

## 確認観点

- 初期リリース方針が `DecapCMS`（SEO補助情報専用）と `BASE` 外部リンク導線に統一されていること
- 初期リリース対象にサイト内カート、チェックアウト、`Shopify` API 連携、本文更新用 `microCMS`、`preview/` 別アプリが混入していないこと
- 問い合わせ通知先、問い合わせログ、rate-limit 情報の責務が `DecapCMS` から分離され、公開Webルート外保存の方針になっていること
- 配送、返品、特商法、EC利用規約の正本が `BASE` 側であること
- README、Agent、基本設計、詳細設計、web_mock ロードマップが現行方針と矛盾していないこと
- 実ディレクトリ構成と README / Agent の説明が矛盾していないこと

## 修正済み事項

- 旧方針の `microCMS` / `Shopify` / カート前提を初期リリース方針から除外した
- `DecapCMS` はSEO補助情報専用、本文系コンテンツは静的データ管理、将来 `microCMS` 移行という表現に統一した
- ECは `BASE` 商品ページへの外部リンク導線に統一し、サイト内カート・チェックアウトを作らない方針に統一した
- `preview/` 別アプリは初期リリースでは作らず、将来 `microCMS` 移行時に再検討する方針に統一した
- 問い合わせ通知先、ログ、rate-limit 保存先、許可オリジン、スパム対策の責務を問い合わせAPI側へ寄せた
- README / Agent / docs README のフォルダ説明を実ディレクトリ構成と計画中ディレクトリの扱いに合わせた
- `web_mock` の Static Export 方針に合わせ、`@next/next/no-img-element` 警告をモックアプリ側だけ無効化した

## 検証結果

- 横断検索で残る `microCMS` / `Shopify` / `preview/` / `checkout` / カート関連記述は、将来移行または履歴説明に限定されている
- `web/` の `npm run build` は成功した
- `web_mock/` の `npm run build` は成功し、ESLint 警告も解消した
- `git diff --check` は空白エラーなしで完了した

## 残指摘

なし。

